<div class="row mb-4">
  <div class="col">
    <h1 class="display-4">Editar Ordem de Serviço #<%= @service_order.id %></h1>
    <p class="lead">Atualize os dados da ordem de serviço.</p>
  </div>
</div>

<div class="card">
  <div class="card-body">
    <%= render "form", service_order: @service_order %>
  </div>
</div>
